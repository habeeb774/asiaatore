# دليل المكونات الموحدة في المشروع

## نظرة عامة
تم توحيد جميع المكونات المكررة في المشروع لتحسين قابلية الصيانة وتجنب التكرار. جميع المكونات الموحدة تدعم اللغات المتعددة والوضع المظلم وتأثيرات بصرية متقدمة.

## المكونات الموحدة

### 1. HeroSlider - مكون البطلة الموحد
**الموقع:** `client/src/components/HeroSlider.jsx`

**الميزات:**
- عرض شرائح متعددة مع تأثيرات انتقال سلسة
- دعم الصور والفيديوهات
- أزرار تفاعلية قابلة للتخصيص
- دعم اللغات المتعددة مع تعليقات عربية
- تحسينات الأداء مع التحميل الكسول

**الاستخدام:**
```jsx
import HeroSlider from './components/HeroSlider';

const slides = [
  {
    id: 1,
    title: { ar: 'عنوان بالعربية', en: 'Title in English' },
    description: { ar: 'وصف بالعربية', en: 'Description in English' },
    image: '/path/to/image.jpg',
    buttons: [
      { text: { ar: 'تسوق الآن', en: 'Shop Now' }, link: '/shop' }
    ]
  }
];

<HeroSlider slides={slides} />
```

### 2. FooterUnified - مكون التذييل الموحد
**الموقع:** `client/src/components/FooterUnified.jsx`

**الميزات:**
- روابط منظمة في أقسام
- معلومات الاتصال والتواصل الاجتماعي
- تطبيقات الهاتف المحمول
- طرق الدفع والشحن
- شهادات الأمان والجودة
- دعم اللغات المتعددة

**الاستخدام:**
```jsx
import FooterUnified from './components/FooterUnified';

<FooterUnified />
```

### 3. NavigationUnified - مكون التنقل الموحد
**الموقع:** `client/src/components/NavigationUnified.jsx`

**الميزات:**
- تنقل متجاوب مع قائمة محمولة
- شريط جانبي قابل للطي
- بحث ذكي مع اقتراحات
- عربة التسوق مع عداد
- تبديل الوضع المظلم
- دعم اللغات المتعددة

**الاستخدام:**
```jsx
import NavigationUnified from './components/NavigationUnified';

<NavigationUnified />
```

### 4. ModalUnified - مكون النوافذ المنبثقة الموحد
**الموقع:** `client/src/components/ModalUnified.jsx`

**الميزات:**
- أنواع متعددة من النوافذ (تأكيد، تحذير، نجاح، معلومات، خطأ)
- تأثيرات بصرية متقدمة مع Framer Motion
- إغلاق بالضغط خارج النافذة أو ESC
- دعم المحتوى المخصص
- أزرار قابلة للتخصيص

**الاستخدام:**
```jsx
import Modal, { ConfirmModal, SuccessModal } from './components/ModalUnified';

// نافذة تأكيد
<ConfirmModal
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  onConfirm={handleConfirm}
  title="تأكيد العملية"
>
  هل أنت متأكد من رغبتك في المتابعة؟
</ConfirmModal>

// نافذة نجاح
<SuccessModal
  isOpen={showSuccess}
  onClose={() => setShowSuccess(false)}
  title="تم بنجاح"
>
  تم إكمال العملية بنجاح!
</SuccessModal>
```

### 5. SidebarUnified - مكون الشريط الجانبي الموحد
**الموقع:** `client/src/components/SidebarUnified.jsx`

**الميزات:**
- أنواع متعددة (عربة، قائمة، مستخدم، مفضلة)
- إدارة المنتجات في العربة
- معلومات المستخدم والحساب
- روابط سريعة
- دعم اللغات المتعددة

**الاستخدام:**
```jsx
import SidebarUnified from './components/SidebarUnified';

// عربة التسوق
<SidebarUnified
  isOpen={cartOpen}
  onClose={() => setCartOpen(false)}
  type="cart"
/>

// معلومات المستخدم
<SidebarUnified
  isOpen={userMenuOpen}
  onClose={() => setUserMenuOpen(false)}
  type="user"
/>
```

### 6. NotificationUnified - مكون الإشعارات الموحد
**الموقع:** `client/src/components/NotificationUnified.jsx`

**الميزات:**
- أنواع متعددة من الإشعارات (نجاح، خطأ، تحذير، معلومات)
- إشعارات مؤقتة ودائمة
- إدارة متعددة الإشعارات
- تأثيرات بصرية متقدمة
- دعم اللغات المتعددة

**الاستخدام:**
```jsx
import { useNotificationContext } from './components/NotificationUnified';

const { showSuccess, showError, showWarning, showInfo } = useNotificationContext();

// إشعار نجاح
showSuccess('تم حفظ البيانات بنجاح!');

// إشعار خطأ
showError('حدث خطأ في حفظ البيانات.');

// إشعار تحذير
showWarning('تحقق من صحة البيانات المدخلة.');

// إشعار معلومات
showInfo('تم تحديث النظام بنجاح.');
```

### 7. ProductCardUnified - مكون بطاقة المنتج الموحد
**الموقع:** `client/src/components/shared/ProductCard/ProductCardUnified.jsx`

**الميزات:**
- أنواع متعددة من البطاقات (افتراضي، مصغر، مميز، شبكة، قائمة)
- عرض سريع للمنتج
- إدارة العربة مع الكميات
- المفضلة والتقييمات
- تأثيرات بصرية متقدمة

**الاستخدام:**
```jsx
import ProductCard, { ProductCardSkeleton, ProductCardGrid } from './components/shared/ProductCard';

// بطاقة منتج عادية
<ProductCard
  product={product}
  variant="default"
  showQuickView={true}
  showAddToCart={true}
/>

// بطاقة منتج مميزة
<ProductCard
  product={product}
  variant="featured"
  priority={true}
/>

// شبكة المنتجات
<ProductCardGrid columns={{ default: 1, md: 2, lg: 3, xl: 4 }}>
  {products.map(product => (
    <ProductCard key={product.id} product={product} />
  ))}
</ProductCardGrid>

// هيكل عظمي للتحميل
<ProductCardSkeleton variant="default" />
```

## أفضل الممارسات

### 1. استخدام الخطافات المخصصة
```jsx
// بدلاً من استخدام useState مباشرة
const { showSuccess, showError } = useNotificationContext();

// استخدم الخطافات المخصصة للإشعارات
showSuccess('تم بنجاح!');
```

### 2. تمرير الخصائص بشكل صحيح
```jsx
// استخدم الخصائص المطلوبة فقط
<ProductCard
  product={product}
  variant="featured"
  showQuickView={true}
  priority={index < 4} // للتحسين الأداء
/>
```

### 3. دعم اللغات المتعددة
```jsx
// استخدم الكائنات للنصوص متعددة اللغات
const title = {
  ar: 'العنوان بالعربية',
  en: 'Title in English'
};

// أو استخدم خطاف اللغة
const { t } = useLanguage();
const title = t('product.title');
```

### 4. تحسينات الأداء
```jsx
// استخدم React.memo للمكونات الثابتة
const ProductCard = memo(({ product, ...props }) => {
  // المحتوى
});

// استخدم useMemo للحسابات المكلفة
const processedData = useMemo(() => {
  return expensiveCalculation(product);
}, [product]);
```

### 5. إمكانية الوصول
```jsx
// أضف aria-label للأزرار التفاعلية
<button
  aria-label={locale === 'ar' ? 'إضافة للعربة' : 'Add to cart'}
  onClick={handleAddToCart}
>
  <ShoppingCartIcon />
</button>
```

## الترقية من المكونات القديمة

### استبدال المكونات القديمة
```jsx
// القديم
import Hero from './components/Hero';
import Footer from './components/Footer';

// الجديد
import HeroSlider from './components/HeroSlider';
import FooterUnified from './components/FooterUnified';
```

### تحديث الخصائص
```jsx
// إذا كانت الخصائص مختلفة، حدثها حسب التوثيق الجديد
<HeroSlider slides={slides} autoPlay={true} />
```

## الاختبار والتحقق

### اختبار المكونات
```jsx
// استخدم React Testing Library
import { render, screen } from '@testing-library/react';
import ProductCard from './components/shared/ProductCard';

test('renders product card', () => {
  const product = { id: 1, name: 'Test Product', price: 100 };
  render(<ProductCard product={product} />);
  
  expect(screen.getByText('Test Product')).toBeInTheDocument();
});
```

### فحص الأداء
```jsx
// استخدم React DevTools Profiler
// أو Lighthouse للتحقق من الأداء
```

## الدعم والصيانة

### إضافة ميزات جديدة
1. أضف الخصائص الجديدة مع قيم افتراضية
2. حدث التوثيق
3. اختبر التغييرات
4. تأكد من التوافق مع الإصدارات السابقة

### إصلاح الأخطاء
1. تحقق من console للأخطاء
2. استخدم React DevTools للتتبع
3. اختبر في جميع المتصفحات
4. تأكد من دعم الشاشات المختلفة

## الخلاصة

المكونات الموحدة توفر:
- 🔧 **سهولة الصيانة** - كود منظم وموحد
- 🚀 **أداء محسن** - تحسينات وmemoization
- 🌍 **دعم متعدد اللغات** - عربي وإنجليزي
- 🌙 **وضع داكن** - تجربة مستخدم محسنة
- 📱 **تصميم متجاوب** - يعمل على جميع الأجهزة
- ♿ **إمكانية وصول** - دعم التقنيات المساعدة
- 🎨 **تأثيرات بصرية** - واجهة حديثة وجذابة

استخدم هذا الدليل كمرجع لتطوير وصيانة المكونات في المشروع.