# ===============================
# 🌐 Database Connection
# ===============================
DATABASE_URL=mysql://root:AlvYFhUfDYXSCykrgHpncurIFgwffLmF@yamabiko.proxy.rlwy.net:23471/railway
# Optional aliases (not required by the app, but common in providers)
MYSQL_URL=${DATABASE_URL}
MYSQL_PUBLIC_URL=${DATABASE_URL}
MYSQL_ROOT_PASSWORD=AlvYFhUfDYXSCykrgHpncurIFgwffLmF
# ===============================
# 🔐 Authentication / Tokens
# ===============================
AUTH_SECRET='manafadasiacompany' # مثال: s9#rG@8nF!Qkz1tL
JWT_SECRET=${AUTH_SECRET}
COOKIE_SECRET=${AUTH_SECRET}
COOKIE_SAMESITE=lax
# ===============================
# 🌐 API / Backend URL (Client)
# In production, the built SPA should call the same origin using a relative path.
# Vite (dev) uses VITE_PROXY_TARGET locally; in prod, keep VITE_API_URL relative.
VITE_PROXY_TARGET=https://localhost:4000
VITE_API_URL=/api
# ===============================
# 🌍 CORS / Frontend Origins
# ===============================
# Comma-separated allowlist for production CORS
CORS_ORIGIN=https://manafadasiacompany.store
# Optional informational alias (not used directly by server code)
CLIENT_ORIGIN=https://manafadasiacompany.store

# Base URL used for links in emails (verification, reset)
APP_BASE_URL=https://manafadasiacompany.store

# ===============================
# 💰 PayPal Configuration
# ===============================
PAYPAL_CLIENT_ID=<YOUR_PAYPAL_CLIENT_ID>
PAYPAL_SECRET=<YOUR_PAYPAL_SECRET>
# Sandbox (اختبار): https://api-m.sandbox.paypal.com
# Production (فعلي): https://api-m.paypal.com
PAYPAL_API=https://api-m.sandbox.paypal.com
PAYPAL_RETURN_URL=https://manafadasiacompany.store/checkout/success
PAYPAL_CANCEL_URL=https://manafadasiacompany.store/checkout/cancel

# ===============================
# 🏦 Bank (Manual Payment)
# ===============================
BANK_ACCOUNT_NAME=Manaf Ad Asia Company

# ===============================
# ⚙️ Environment / Serving built client
# ===============================
NODE_ENV=production
# If you deploy the Node server and want it to serve the built SPA from ./dist
SERVE_CLIENT=true
# In managed hosts (Railway, etc.) PORT is provided by the platform


# ===============================
# 🧠 Optional Advanced
# ===============================
# Token TTLs as used by the server (jwt.js)
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
# Backward-compatible aliases (supported as fallback)
# ACCESS_TOKEN_EXPIRES_IN=15m
# REFRESH_TOKEN_EXPIRES_IN=7d

# Rate limits (enabled by default in production). Uncomment to override.
# RATE_LIMIT_AUTH_ENABLE=true
# RATE_LIMIT_PAY_ENABLE=true
