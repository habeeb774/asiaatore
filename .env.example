############################################
# Example environment configuration template
############################################

########################
# Database (choose ONE)
########################
# Option 1: Direct URL (preferred)
DATABASE_URL="mysql://root:@localhost:3306/my_store_fresh"

# Option 2: Parts (server will assemble a mysql:// URL if DATABASE_URL missing)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASS=
# DB_NAME=my_store_db

########################
# Auth & Server
########################
AUTH_SECRET="change_this_secret"
PORT=4003
########################
# PayPal Sandbox Credentials
########################
# Obtain from: https://developer.paypal.com/ (Apps & Credentials - Sandbox)
# Never commit real production secrets.
PAYPAL_CLIENT_ID=your_paypal_sandbox_client_id
PAYPAL_SECRET=your_paypal_sandbox_secret
PAYPAL_API=https://api-m.sandbox.paypal.com
# Frontend base typically Vite: 5173 (adjust if needed)
PAYPAL_RETURN_URL=http://localhost:5173/checkout/success
PAYPAL_CANCEL_URL=http://localhost:5173/checkout/cancel
# Set to false to disable automatic currency fallback from SAR -> USD
# PAYPAL_FALLBACK_USD=true

########################
# STC Pay (Simulation layer)
########################
# Currently simulated; keep for future real integration toggles
# STC_API_KEY=
# STC_API_BASE=https://sandbox.stcpay.example

########################
# Bank Transfer (Manual Payment)
########################
BANK_ACCOUNT_NAME="Demo Trading Co."
BANK_ACCOUNT_IBAN="SA03 8000 0000 6080 1016 7519"
BANK_NAME="Demo Bank"

########################
# Developer Helpers (DO NOT use in production)
########################
# QUICK_START_DB=1          # Auto inject fallback mysql://root:root@localhost:3306/my_store
# ALLOW_INVALID_DB=true     # Run in degraded mode without a valid DB
# DEBUG_ERRORS=true         # Return more verbose API error info

########################
# Optional Logging / Debug
########################
# LOG_SQL=true             # (If you add Prisma logging logic later)
# DEBUG_PAYMENTS=true      # (Future hook for verbose payment logs)

############################################
# Notes:
# - Copy this file to .env and fill the required secrets.
# - Do NOT commit filled .env to version control.
# - After edits: restart the backend server to apply changes.
############################################
