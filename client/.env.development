# Local dev: point Vite proxy to the API started on 8829
VITE_PROXY_TARGET=http://localhost:4000
# Local dev: point Vite proxy to the API port
# If the API is running on 4000 (default), use that; change to 8829 only if you explicitly start the API there
VITE_API_URL=/api
