import React from 'react';

// Lightweight placeholder to avoid breaking imports.
// In the full version, this would guide an admin through initial store configuration.
export default function AdminSetupModal() {
  return null;
}
