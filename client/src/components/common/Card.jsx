// Unified: re-export the canonical UI Card primitives to avoid duplication.
// Keep this compatibility file so existing imports from "components/common/Card" continue to work.
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from '../ui/Card.jsx';
